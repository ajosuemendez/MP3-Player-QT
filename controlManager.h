#ifndef CONTROLMANAGER_H
#define CONTROLMANAGER_H
/*
#include "library.h"
#include "song.h"

class controlManager{

    enum class State{Stop, Playing, Paused};
    Library *stack;
    State currentState;

public:
    //State getCurrentState();
    void startPlaying(Song song);
    void resumePlaying();
    void stopPlaying();
    //void setNextSong(Song song);
    //void sortSongs(Library lib);
    //Song searchSong(QString songName);

public:
    controlManager();
    controlManager(Library *lib);

};

#endif // CONTROLMANAGER_H
*/
